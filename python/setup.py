# build with: python setup.py install --user

from distutils.core import setup

setup(
    name='mcapi',
    version='0.5',
    packages=['mcapi']
)

setup(
    name='casm_mcapi',
    version='0.2',
    packages=['casm_mcapi']
)