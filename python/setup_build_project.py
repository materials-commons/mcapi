# build with: python setup.py install --user

from distutils.core import setup

setup(
    name='mcapi',
    version='0.5',
    packages=['mcapi']
)

setup(
    name='demo_project',
    version='0.2',
    packages=['demo_project']
)
